<?php

namespace {{ baseNamespace }}\Controller;

use App\Application\Project\ContentBundle\Controller\DefaultCRUDController;
use App\Application\Project\ContentBundle\Attributes\ARR;
use Sonata\AdminBundle\Datagrid\ProxyQueryInterface;
use Symfony\Component\HttpFoundation\Response;
use Symfony\Component\HttpFoundation\Request;

class {{ className }}AdminController extends DefaultCRUDController
{
    public function __construct(){}

    #[ARR(routerName: 'listAction', role: "ROLE_ADMIN_{{ className|upper }}_LIST", title: 'Listar')]
    public function listAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_LIST");

        return parent::listAction($request);
    }

    #[ARR(routerName: 'showAction', role: "ROLE_ADMIN_{{ className|upper }}_SHOW", title: 'Visualizar')]
    public function showAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_SHOW");

        return parent::showAction($request);
    }

    #[ARR(routerName: 'createAction', role: "ROLE_ADMIN_{{ className|upper }}_CREATE", title: 'Criar')]
    public function createAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_CREATE");

        return parent::createAction($request);
    }

    #[ARR(routerName: 'editAction', role: "ROLE_ADMIN_{{ className|upper }}_EDIT", title: 'Editar')]
    public function editAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_EDIT");

        return parent::editAction($request);
    }

    #[ARR(routerName: 'deleteAction', role: "ROLE_ADMIN_{{ className|upper }}_DELETE", title: 'Excluir')]
    public function deleteAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_DELETE");

        return parent::deleteAction($request);
    }

    #[ARR(routerName: 'batchAction', role: "ROLE_ADMIN_{{ className|upper }}_BATCH", title: 'Excluir em Lote')]
    public function batchActionDelete(ProxyQueryInterface $query): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_BATCH");

        return parent::batchActionDelete($query);
    }

    public function batchAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_BATCH");

        return parent::batchAction($request);
    }

    #[ARR(routerName: 'exportAction', role: "ROLE_ADMIN_{{ className|upper }}_EXPORT", title: 'Exportar')]
    public function exportAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_EXPORT");

        return parent::exportAction($request);
    }

    #[ARR(routerName: 'historyAction', role: "ROLE_ADMIN_{{ className|upper }}_HISTORY", title: 'Auditoria')]
    public function historyAction(Request $request): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_HISTORY");

        return parent::historyAction($request);
    }

    public function historyViewRevisionAction(Request $request, string $revision): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_HISTORY");

        return parent::historyViewRevisionAction($request, $revision);
    }

    public function historyCompareRevisionsAction(Request $request, string $baseRevision, string $compareRevision): Response
    {
        $this->validateAccess("ROLE_ADMIN_{{ className|upper }}_HISTORY");

        return parent::historyCompareRevisionsAction($request, $baseRevision, $compareRevision);
    }

}