{% if attribute.typeDoctrine == ""  %}
{% endif %}
        $list->addIdentifier('{{ attribute.attributeName|raw }}', null, [
            'label' => '{{ attribute.attributeName|capitalize }}',
        ]);